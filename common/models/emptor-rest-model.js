/**
 * Emptorrestmodel.
 */
module.exports = function (Emptorrestmodel) {
  console.log('pending.')
  /**
   * @param {string} clientId
   * @param {number} cb - this is a callback.
   * @return {string} result of the description function.
   */
  /* Emptorrestmodel.getUserData = function (accessToken, cb) {
    console.log('buuu')
    Emptorrestmodel.find(accessToken, function (err, data) {
      // if error
      if (err) {
        return { status: 401, message: err }
      } else {
        return cb(null, data)
      }
    })
  } */
}
